<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Serenata Virtual 💖</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #fff;
      overflow: hidden;
    }

    .card {
      background: rgba(0, 0, 0, 0.35);
      padding: 30px;
      border-radius: 20px;
      text-align: center;
      max-width: 420px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.3);
      animation: fadeIn 2s ease;
    }

    p {
      font-size: 1.1em;
      line-height: 1.7;
    }

    .heart {
      position: absolute;
      bottom: -10vh;
      color: gold;
      font-size: 40px;
      text-shadow: 0 0 10px rgba(255, 215, 0, 0.9);
      animation: floatUpSingle 6s ease-out forwards;
    }

    img {
      width:170px;
      height:170px;
      border-radius:50%;
      object-fit:cover;
      object-position:center;
      box-shadow:0 8px 20px rgba(0,0,0,0.45);
      margin:15px 0;
      display:none;
    }

    input[type="file"] {
      margin-top: 10px;
      color: #fff;
    }

    @keyframes floatUpSingle {
      0% { transform: translateY(0) scale(0.8); opacity: 0; }
      20% { opacity: 1; }
      100% { transform: translateY(-80vh) scale(1.2); opacity: 0; }
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: scale(0.9); }
      to { opacity: 1; transform: scale(1); }
    }
  </style>
</head>
<body>

  <iframe id="music" width="0" height="0" src="" allow="autoplay" style="display:none;"></iframe>

  <div class="card">
    <p style="font-size:1.15em;opacity:0.95;">
      💌 Antes de tocar el corazón…<br>
      cierra los ojos un segundo,<br>
      piensa en nosotros<br>
      y prepárate para sentir cuánto te amo 💛✨
    </p>
    <button onclick="startSerenade()" style="background:none;border:none;font-size:40px;cursor:pointer;">❤️</button>

    <img id="foto" alt="Nuestra foto" />

    <input type="file" id="photoInput" accept="image/*" />

    <p id="message" style="display:none;">
      Feliz cumpleaños, amorcito mío 🤍<br><br>
      Hoy celebramos tus 21 años, pero yo celebro mucho más que eso… celebro el privilegio de tenerte en mi vida. Desde que llegaste, todo es más bonito, más tranquilo, más sincero.<br><br>
      Me enamora tu forma de ser, tu sonrisa que cura cualquier día difícil, tu voz que me da paz y tu corazón tan puro. No sabes lo feliz que me hace verte sonreír, cuidarte, escucharte y soñar contigo.<br><br>
      Deseo que este nuevo año te abrace con cosas buenas, que nunca te falte amor, y que cuando dudes, recuerdes que aquí estoy yo, para sostenerte la mano siempre.<br><br>
      Gracias por existir, por ser tú, por dejarme amarte.<br>
      Feliz cumpleaños, mi niña bonita 🎂✨<br><br>
      <strong>Te amo más de lo que las palabras saben decir 🤍</strong>
    </p>
  </div>

  <script>
    const photoInput = document.getElementById('photoInput');
    const foto = document.getElementById('foto');

    photoInput.addEventListener('change', () => {
      const file = photoInput.files[0];
      if (!file) return;
      const reader = new FileReader();
      reader.onload = e => {
        foto.src = e.target.result;
        foto.style.display = 'block';
      };
      reader.readAsDataURL(file);
    });

    function startSerenade() {
      const iframe = document.getElementById('music');
      iframe.src = 'https://www.youtube.com/embed/UuGrc3vvdMw?autoplay=1';

      document.getElementById('message').style.display = 'block';
      createHearts();
    }

    function createHearts() {
      const heart = document.createElement('div');
      heart.className = 'heart';
      heart.innerHTML = '💛';
      heart.style.left = Math.random() * 100 + 'vw';
      document.body.appendChild(heart);

      setTimeout(() => {
        heart.remove();
      }, 6000);
    }

    // Test básico: crear un corazón automático cada 5s
    setInterval(createHearts, 5000);
  </script>

</body>
</html>
